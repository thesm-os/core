// Code generated by testkit stub. DO NOT EDIT.
// Source: keeper.go:38-51 (testkit stub Keeper)

package cryptotest

import (
	"context"
	"testing"

	"go.thesmos.sh/core/crypto"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// Compile-time interface check.
var _ crypto.Keeper = (*KeeperStub)(nil)

// --- Call types ---

// KeeperKeyIDCall records one invocation of crypto.Keeper.KeyID.
// Result fields use the return-value names from the source interface when
// available; unnamed returns produce positional names (Result0, Result1, ...).
type KeeperKeyIDCall struct {
	Result string
}

// KeeperUnwrapCall records one invocation of crypto.Keeper.Unwrap.
// Result fields use the return-value names from the source interface when
// available; unnamed returns produce positional names (Result0, Result1, ...).
type KeeperUnwrapCall struct {
	Ctx     context.Context
	Wrapped []byte
	Result  []byte
	Err     error
}

// KeeperWrapCall records one invocation of crypto.Keeper.Wrap.
// Result fields use the return-value names from the source interface when
// available; unnamed returns produce positional names (Result0, Result1, ...).
type KeeperWrapCall struct {
	Ctx    context.Context
	Dek    []byte
	Result []byte
	Err    error
}

// --- Per-method stubs ---

// KeeperKeyIDStub controls behavior and records calls for KeyID.
//
// Concurrent-test primitives inherited from [stub.MethodStub]:
//   - [stub.Recorder.WaitForN] / [stub.Recorder.WaitFor] — block until calls arrive (cond-variable, no polling)
//   - [stub.Recorder.NewGate] / [stub.Gate.Release] / [stub.Gate.ReleaseOne] — deterministic race control
//   - [stub.Recorder.OnRecord] — streaming hook fired synchronously on every call
type KeeperKeyIDStub struct {
	*stub.MethodStub[KeeperKeyIDCall]
	fn       func() string
	fallback *keeperKeyIDReturn
}

type keeperKeyIDReturn struct {
	Result string
}

// Returns sets a fixed return value for KeyID.
func (s *KeeperKeyIDStub) Returns(result string) *KeeperKeyIDStub {
	s.fallback = &keeperKeyIDReturn{Result: result}
	return s
}

// Func sets a function override for KeyID.
func (s *KeeperKeyIDStub) Func(fn func() string) *KeeperKeyIDStub {
	s.fn = fn
	return s
}

// KeeperUnwrapStub controls behavior and records calls for Unwrap.
//
// Concurrent-test primitives inherited from [stub.MethodStub]:
//   - [stub.Recorder.WaitForN] / [stub.Recorder.WaitFor] — block until calls arrive (cond-variable, no polling)
//   - [stub.Recorder.NewGate] / [stub.Gate.Release] / [stub.Gate.ReleaseOne] — deterministic race control
//   - [stub.Recorder.OnRecord] — streaming hook fired synchronously on every call
type KeeperUnwrapStub struct {
	*stub.MethodStub[KeeperUnwrapCall]
	fn       func(context.Context, []byte) ([]byte, error)
	fallback *keeperUnwrapReturn
}

type keeperUnwrapReturn struct {
	Result []byte
	Err    error
}

// Returns sets a fixed return value for Unwrap.
func (s *KeeperUnwrapStub) Returns(result []byte, err error) *KeeperUnwrapStub {
	s.fallback = &keeperUnwrapReturn{Result: result, Err: err}
	return s
}

// Func sets a function override for Unwrap.
func (s *KeeperUnwrapStub) Func(fn func(context.Context, []byte) ([]byte, error)) *KeeperUnwrapStub {
	s.fn = fn
	return s
}

// KeeperWrapStub controls behavior and records calls for Wrap.
//
// Concurrent-test primitives inherited from [stub.MethodStub]:
//   - [stub.Recorder.WaitForN] / [stub.Recorder.WaitFor] — block until calls arrive (cond-variable, no polling)
//   - [stub.Recorder.NewGate] / [stub.Gate.Release] / [stub.Gate.ReleaseOne] — deterministic race control
//   - [stub.Recorder.OnRecord] — streaming hook fired synchronously on every call
type KeeperWrapStub struct {
	*stub.MethodStub[KeeperWrapCall]
	fn       func(context.Context, []byte) ([]byte, error)
	fallback *keeperWrapReturn
}

type keeperWrapReturn struct {
	Result []byte
	Err    error
}

// Returns sets a fixed return value for Wrap.
func (s *KeeperWrapStub) Returns(result []byte, err error) *KeeperWrapStub {
	s.fallback = &keeperWrapReturn{Result: result, Err: err}
	return s
}

// Func sets a function override for Wrap.
func (s *KeeperWrapStub) Func(fn func(context.Context, []byte) ([]byte, error)) *KeeperWrapStub {
	s.fn = fn
	return s
}

// --- KeeperStub ---

// KeeperStubOption configures a [KeeperStub] at construction time.
type KeeperStubOption func(*KeeperStub)

// KeeperStubStrict enables strict mode — unconfigured methods fail the test.
func KeeperStubStrict() KeeperStubOption {
	return func(s *KeeperStub) { s.strict = true }
}

// KeeperStubDelegateTo wraps a real implementation — all calls delegate to
// impl and are recorded on the outer stub.
func KeeperStubDelegateTo(impl crypto.Keeper) KeeperStubOption {
	return func(s *KeeperStub) {
		s.OnKeyID.Func(impl.KeyID)
		s.OnUnwrap.Func(impl.Unwrap)
		s.OnWrap.Func(impl.Wrap)
	}
}

// KeeperStubWithClock injects a [clock.Clock] into all per-method stubs.
// Use [clock.NewTestClock] for deterministic virtual time or a consumer's
// own clock implementation. Default is real wall-clock time.
func KeeperStubWithClock(clk clock.Clock) KeeperStubOption {
	return func(s *KeeperStub) {
		s.OnKeyID.WithClock(clk)
		s.OnUnwrap.WithClock(clk)
		s.OnWrap.WithClock(clk)
	}
}

// KeeperStubWithRandSource injects a [rand.Source] into all per-method
// stubs. Use [rand.FixedRandSource] for deterministic testing or a consumer's
// own seeded RNG. Default is [rand.DefaultRandSource] (math/rand/v2).
func KeeperStubWithRandSource(src rand.Source) KeeperStubOption {
	return func(s *KeeperStub) {
		s.OnKeyID.WithRandSource(src)
		s.OnUnwrap.WithRandSource(src)
		s.OnWrap.WithRandSource(src)
	}
}

// KeeperStubBenchMode disables call recording on all per-method stubs. Use
// this in benchmarks to avoid allocation overhead from accumulated call logs.
// Dispatch (Func, Returns, Faults) still works normally.
func KeeperStubBenchMode() KeeperStubOption {
	return func(s *KeeperStub) {
		s.OnKeyID.BenchMode()
		s.OnUnwrap.BenchMode()
		s.OnWrap.BenchMode()
	}
}

// WithKeeperKeyID sets a function override at construction time.
func WithKeeperKeyID(fn func() string) KeeperStubOption {
	return func(s *KeeperStub) { s.OnKeyID.Func(fn) }
}

// WithKeeperUnwrap sets a function override at construction time.
func WithKeeperUnwrap(fn func(context.Context, []byte) ([]byte, error)) KeeperStubOption {
	return func(s *KeeperStub) { s.OnUnwrap.Func(fn) }
}

// WithKeeperWrap sets a function override at construction time.
func WithKeeperWrap(fn func(context.Context, []byte) ([]byte, error)) KeeperStubOption {
	return func(s *KeeperStub) { s.OnWrap.Func(fn) }
}

// KeeperStub is a configurable test double for [crypto.Keeper].
type KeeperStub struct {
	OnKeyID  *KeeperKeyIDStub
	OnUnwrap *KeeperUnwrapStub
	OnWrap   *KeeperWrapStub
	strict   bool
}

// NewKeeperStub creates a stub. Pass tb for auto-verification of
// expectations at test cleanup. Pass nil for a pure stub.
func NewKeeperStub(tb testing.TB, opts ...KeeperStubOption) *KeeperStub {
	s := &KeeperStub{
		OnKeyID:  &KeeperKeyIDStub{MethodStub: stub.NewMethodStub[KeeperKeyIDCall](tb, "Keeper.KeyID")},
		OnUnwrap: &KeeperUnwrapStub{MethodStub: stub.NewMethodStub[KeeperUnwrapCall](tb, "Keeper.Unwrap")},
		OnWrap:   &KeeperWrapStub{MethodStub: stub.NewMethodStub[KeeperWrapCall](tb, "Keeper.Wrap")},
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.OnKeyID.Strict()
		s.OnUnwrap.Strict()
		s.OnWrap.Strict()
	}
	if tb != nil {
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				s.OnKeyID.Verify()
				s.OnUnwrap.Verify()
				s.OnWrap.Verify()
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count expectations.
// Func, Returns, and Faults configuration is preserved.
func (s *KeeperStub) ResetCalls() {
	s.OnKeyID.Reset()
	s.OnUnwrap.Reset()
	s.OnWrap.Reset()
}

// --- Interface method implementations ---

func (s *KeeperStub) KeyID() string {
	s.OnKeyID.SleepLatency()
	call := KeeperKeyIDCall{}
	if s.OnKeyID.fn != nil {
		r0 := s.OnKeyID.fn()
		call.Result = r0
		s.OnKeyID.Record(call)
		return r0
	}
	if s.OnKeyID.fallback != nil {
		f := s.OnKeyID.fallback
		call.Result = f.Result
		s.OnKeyID.Record(call)
		return f.Result
	}
	s.OnKeyID.FailUnexpectedCall(call)
	s.OnKeyID.Record(call)
	return ""
}

func (s *KeeperStub) Unwrap(ctx context.Context, wrapped []byte) ([]byte, error) {
	s.OnUnwrap.SleepLatency()
	call := KeeperUnwrapCall{Ctx: ctx, Wrapped: wrapped}
	if fired, faultErr := s.OnUnwrap.ShouldFaultFor(call); fired {
		call.Err = faultErr
		s.OnUnwrap.Record(call)
		return nil, faultErr
	}
	if s.OnUnwrap.fn != nil {
		r0, r1 := s.OnUnwrap.fn(ctx, wrapped)
		call.Result = r0
		call.Err = r1
		s.OnUnwrap.Record(call)
		return r0, r1
	}
	if s.OnUnwrap.fallback != nil {
		f := s.OnUnwrap.fallback
		call.Result = f.Result
		call.Err = f.Err
		s.OnUnwrap.Record(call)
		return f.Result, f.Err
	}
	s.OnUnwrap.FailUnexpectedCall(call)
	s.OnUnwrap.Record(call)
	return nil, nil
}

func (s *KeeperStub) Wrap(ctx context.Context, dek []byte) ([]byte, error) {
	s.OnWrap.SleepLatency()
	call := KeeperWrapCall{Ctx: ctx, Dek: dek}
	if fired, faultErr := s.OnWrap.ShouldFaultFor(call); fired {
		call.Err = faultErr
		s.OnWrap.Record(call)
		return nil, faultErr
	}
	if s.OnWrap.fn != nil {
		r0, r1 := s.OnWrap.fn(ctx, dek)
		call.Result = r0
		call.Err = r1
		s.OnWrap.Record(call)
		return r0, r1
	}
	if s.OnWrap.fallback != nil {
		f := s.OnWrap.fallback
		call.Result = f.Result
		call.Err = f.Err
		s.OnWrap.Record(call)
		return f.Result, f.Err
	}
	s.OnWrap.FailUnexpectedCall(call)
	s.OnWrap.Record(call)
	return nil, nil
}
