// Code generated by testkit suite. DO NOT EDIT.
// Source: sign.go:93-115 (testkit suite Verifier)

package cryptotest

import (
	"context"
	"testing"

	"go.thesmos.sh/core/crypto"
	"go.thesmos.sh/core/crypto/sign"
	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/bindings"
	"go.thesmos.sh/testkit/suite"
)

// AssertVerifierContract runs conformance assertions against
// implementations of [sign.Verifier] produced by factory.
//
//	Default subtests: 6 across 4 methods
//	Shapes detected:  Pure (Algorithm, KeyID, PublicKey), Predicate (Verify)
//	Directives:       none
//	Plug-in points:   VerifierOnAlgorithm, VerifierOnKeyID, VerifierOnPublicKey, VerifierOnVerify, VerifierOnAll, VerifierCustom
func AssertVerifierContract(
	t *testing.T,
	factory func() sign.Verifier,
	opts ...VerifierOption,
) {
	t.Helper()
	cfg := newVerifierConfig(opts...)

	runVerifierAlgorithm(t, factory, &cfg)
	runVerifierKeyID(t, factory, &cfg)
	runVerifierPublicKey(t, factory, &cfg)
	runVerifierVerify(t, factory, &cfg)

	for _, a := range cfg.onAll {
		cctx := suite.CrossContext[sign.Verifier]{
			T:             t,
			CrossBindings: bindings.CrossBindings[sign.Verifier]{Factory: factory},
		}
		a(cctx)
	}

	for _, custom := range cfg.custom {
		t.Run(custom.name, func(t *testing.T) {
			t.Parallel()
			custom.fn(t, factory())
		})
	}
}

func runVerifierAlgorithm(t *testing.T, factory func() sign.Verifier, cfg *verifierConfig) {
	t.Helper()

	t.Run("Algorithm/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: Algorithm panicked on zero-value args (%v) — supply sample values via VerifierOnAlgorithm", r)
			}
		}()
		s := factory()
		_ = s.Algorithm()
	})
	if len(cfg.onAlgorithm) > 0 {
		t.Run("Algorithm", func(t *testing.T) {
			prePopFactory := func() sign.Verifier {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[sign.Verifier, crypto.Algorithm]{
				T: t,
				PureBindings: bindings.PureBindings[sign.Verifier, crypto.Algorithm]{
					Factory: prePopFactory,
					Call: func(impl sign.Verifier) crypto.Algorithm {
						return impl.Algorithm()
					},
				},
			}
			for _, a := range cfg.onAlgorithm {
				a(pctx)
			}
		})
	}
}

func runVerifierKeyID(t *testing.T, factory func() sign.Verifier, cfg *verifierConfig) {
	t.Helper()

	t.Run("KeyID/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: KeyID panicked on zero-value args (%v) — supply sample values via VerifierOnKeyID", r)
			}
		}()
		s := factory()
		_ = s.KeyID()
	})
	if len(cfg.onKeyID) > 0 {
		t.Run("KeyID", func(t *testing.T) {
			prePopFactory := func() sign.Verifier {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[sign.Verifier, sign.KeyID]{
				T: t,
				PureBindings: bindings.PureBindings[sign.Verifier, sign.KeyID]{
					Factory: prePopFactory,
					Call: func(impl sign.Verifier) sign.KeyID {
						return impl.KeyID()
					},
				},
			}
			for _, a := range cfg.onKeyID {
				a(pctx)
			}
		})
	}
}

func runVerifierPublicKey(t *testing.T, factory func() sign.Verifier, cfg *verifierConfig) {
	t.Helper()

	t.Run("PublicKey/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: PublicKey panicked on zero-value args (%v) — supply sample values via VerifierOnPublicKey", r)
			}
		}()
		s := factory()
		_ = s.PublicKey()
	})
	if len(cfg.onPublicKey) > 0 {
		t.Run("PublicKey", func(t *testing.T) {
			prePopFactory := func() sign.Verifier {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[sign.Verifier, []byte]{
				T: t,
				PureBindings: bindings.PureBindings[sign.Verifier, []byte]{
					Factory: prePopFactory,
					Call: func(impl sign.Verifier) []byte {
						return impl.PublicKey()
					},
				},
			}
			for _, a := range cfg.onPublicKey {
				a(pctx)
			}
		})
	}
}

func runVerifierVerify(t *testing.T, factory func() sign.Verifier, cfg *verifierConfig) {
	t.Helper()

	t.Run("Verify/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: Verify panicked on zero-value args (%v) — supply sample values via VerifierOnVerify", r)
			}
		}()
		s := factory()
		_ = s.Verify(nil, nil)
	})

	t.Run("Verify/nil message", func(t *testing.T) {
		t.Parallel()
		testkit.AssertNilSafe(t, func() {
			_ = factory().Verify(nil, nil)
		})
	})

	t.Run("Verify/nil signature", func(t *testing.T) {
		t.Parallel()
		testkit.AssertNilSafe(t, func() {
			_ = factory().Verify(nil, nil)
		})
	})
	if len(cfg.onVerify) > 0 {
		t.Run("Verify", func(t *testing.T) {
			prePopFactory := func() sign.Verifier {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PredicateContext[sign.Verifier]{
				T: t,
				PredicateBindings: bindings.PredicateBindings[sign.Verifier]{
					Factory: prePopFactory,
					Call: func(impl sign.Verifier) bool {
						return impl.Verify(nil, nil)
					},
				},
			}
			for _, a := range cfg.onVerify {
				a(pctx)
			}
		})
	}
}

// VerifierOption configures [AssertVerifierContract].
type VerifierOption func(*verifierConfig)

// VerifierPrePopulate runs before subtests that need pre-populated state.
// Called once per subtest against a fresh impl from the factory.
func VerifierPrePopulate(fn func(ctx context.Context, s sign.Verifier)) VerifierOption {
	return func(c *verifierConfig) { c.prePopulate = fn }
}

// VerifierCustom adds a free-form subtest to the contract. Use this for
// contracts not expressible via shape-specific primitives.
func VerifierCustom(name string, fn func(*testing.T, sign.Verifier)) VerifierOption {
	return func(c *verifierConfig) {
		c.custom = append(c.custom, verifierCustomSubtest{name: name, fn: fn})
	}
}

// VerifierOnAlgorithm adds plug-in assertions for the Algorithm method.
func VerifierOnAlgorithm(assertions ...suite.PureAssertion[sign.Verifier, crypto.Algorithm]) VerifierOption {
	return func(c *verifierConfig) {
		c.onAlgorithm = append(c.onAlgorithm, assertions...)
	}
}

// VerifierOnKeyID adds plug-in assertions for the KeyID method.
func VerifierOnKeyID(assertions ...suite.PureAssertion[sign.Verifier, sign.KeyID]) VerifierOption {
	return func(c *verifierConfig) {
		c.onKeyID = append(c.onKeyID, assertions...)
	}
}

// VerifierOnPublicKey adds plug-in assertions for the PublicKey method.
func VerifierOnPublicKey(assertions ...suite.PureAssertion[sign.Verifier, []byte]) VerifierOption {
	return func(c *verifierConfig) {
		c.onPublicKey = append(c.onPublicKey, assertions...)
	}
}

// VerifierOnVerify adds plug-in assertions for the Verify method.
func VerifierOnVerify(assertions ...suite.PredicateAssertion[sign.Verifier]) VerifierOption {
	return func(c *verifierConfig) {
		c.onVerify = append(c.onVerify, assertions...)
	}
}

// VerifierOnAll adds cross-method assertions that span multiple methods.
func VerifierOnAll(assertions ...suite.CrossMethodAssertion[sign.Verifier]) VerifierOption {
	return func(c *verifierConfig) {
		c.onAll = append(c.onAll, assertions...)
	}
}

type verifierCustomSubtest struct {
	name string
	fn   func(*testing.T, sign.Verifier)
}

type verifierConfig struct {
	prePopulate func(context.Context, sign.Verifier)
	custom      []verifierCustomSubtest
	onAll       []suite.CrossMethodAssertion[sign.Verifier]
	onAlgorithm []suite.PureAssertion[sign.Verifier, crypto.Algorithm]
	onKeyID     []suite.PureAssertion[sign.Verifier, sign.KeyID]
	onPublicKey []suite.PureAssertion[sign.Verifier, []byte]
	onVerify    []suite.PredicateAssertion[sign.Verifier]
}

func newVerifierConfig(opts ...VerifierOption) verifierConfig {
	var cfg verifierConfig
	for _, opt := range opts {
		opt(&cfg)
	}
	return cfg
}
