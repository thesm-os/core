// Code generated by testkit stub. DO NOT EDIT.
// Source: generator.go:29-29 (testkit stub Generator)

package idtest

import (
	"testing"

	"go.thesmos.sh/core/id"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// Compile-time interface check.
var _ id.Generator = (*GeneratorStub)(nil)

// --- Call types ---

// GeneratorGenerateCall records one invocation of id.Generator.Generate.
// Result fields use the return-value names from the source interface when
// available; unnamed returns produce positional names (Result0, Result1, ...).
type GeneratorGenerateCall struct {
	Result id.ID
}

// --- Per-method stubs ---

// GeneratorGenerateStub controls behavior and records calls for Generate.
//
// Concurrent-test primitives inherited from [stub.MethodStub]:
//   - [stub.Recorder.WaitForN] / [stub.Recorder.WaitFor] — block until calls arrive (cond-variable, no polling)
//   - [stub.Recorder.NewGate] / [stub.Gate.Release] / [stub.Gate.ReleaseOne] — deterministic race control
//   - [stub.Recorder.OnRecord] — streaming hook fired synchronously on every call
type GeneratorGenerateStub struct {
	*stub.MethodStub[GeneratorGenerateCall]
	fn       func() id.ID
	fallback *generatorGenerateReturn
}

type generatorGenerateReturn struct {
	Result id.ID
}

// Returns sets a fixed return value for Generate.
func (s *GeneratorGenerateStub) Returns(result id.ID) *GeneratorGenerateStub {
	s.fallback = &generatorGenerateReturn{Result: result}
	return s
}

// Func sets a function override for Generate.
func (s *GeneratorGenerateStub) Func(fn func() id.ID) *GeneratorGenerateStub {
	s.fn = fn
	return s
}

// --- GeneratorStub ---

// GeneratorStubOption configures a [GeneratorStub] at construction time.
type GeneratorStubOption func(*GeneratorStub)

// GeneratorStubStrict enables strict mode — unconfigured methods fail the test.
func GeneratorStubStrict() GeneratorStubOption {
	return func(s *GeneratorStub) { s.strict = true }
}

// GeneratorStubDelegateTo wraps a real implementation — all calls delegate to
// impl and are recorded on the outer stub.
func GeneratorStubDelegateTo(impl id.Generator) GeneratorStubOption {
	return func(s *GeneratorStub) {
		s.OnGenerate.Func(impl.Generate)
	}
}

// GeneratorStubWithClock injects a [clock.Clock] into all per-method stubs.
// Use [clock.NewTestClock] for deterministic virtual time or a consumer's
// own clock implementation. Default is real wall-clock time.
func GeneratorStubWithClock(clk clock.Clock) GeneratorStubOption {
	return func(s *GeneratorStub) {
		s.OnGenerate.WithClock(clk)
	}
}

// GeneratorStubWithRandSource injects a [rand.Source] into all per-method
// stubs. Use [rand.FixedRandSource] for deterministic testing or a consumer's
// own seeded RNG. Default is [rand.DefaultRandSource] (math/rand/v2).
func GeneratorStubWithRandSource(src rand.Source) GeneratorStubOption {
	return func(s *GeneratorStub) {
		s.OnGenerate.WithRandSource(src)
	}
}

// GeneratorStubBenchMode disables call recording on all per-method stubs. Use
// this in benchmarks to avoid allocation overhead from accumulated call logs.
// Dispatch (Func, Returns, Faults) still works normally.
func GeneratorStubBenchMode() GeneratorStubOption {
	return func(s *GeneratorStub) {
		s.OnGenerate.BenchMode()
	}
}

// WithGeneratorGenerate sets a function override at construction time.
func WithGeneratorGenerate(fn func() id.ID) GeneratorStubOption {
	return func(s *GeneratorStub) { s.OnGenerate.Func(fn) }
}

// GeneratorStub is a configurable test double for [id.Generator].
type GeneratorStub struct {
	OnGenerate *GeneratorGenerateStub
	strict     bool
}

// NewGeneratorStub creates a stub. Pass tb for auto-verification of
// expectations at test cleanup. Pass nil for a pure stub.
func NewGeneratorStub(tb testing.TB, opts ...GeneratorStubOption) *GeneratorStub {
	s := &GeneratorStub{
		OnGenerate: &GeneratorGenerateStub{MethodStub: stub.NewMethodStub[GeneratorGenerateCall](tb, "Generator.Generate")},
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.OnGenerate.Strict()
	}
	if tb != nil {
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				s.OnGenerate.Verify()
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count expectations.
// Func, Returns, and Faults configuration is preserved.
func (s *GeneratorStub) ResetCalls() {
	s.OnGenerate.Reset()
}

// --- Interface method implementations ---

func (s *GeneratorStub) Generate() id.ID {
	s.OnGenerate.SleepLatency()
	call := GeneratorGenerateCall{}
	if s.OnGenerate.fn != nil {
		r0 := s.OnGenerate.fn()
		call.Result = r0
		s.OnGenerate.Record(call)
		return r0
	}
	if s.OnGenerate.fallback != nil {
		f := s.OnGenerate.fallback
		call.Result = f.Result
		s.OnGenerate.Record(call)
		return f.Result
	}
	s.OnGenerate.FailUnexpectedCall(call)
	s.OnGenerate.Record(call)
	return id.ID{}
}
