// Code generated by testkit suite. DO NOT EDIT.
// Source: reporter.go:44-59 (testkit suite Reporter)

package telemetrytest

import (
	"context"
	"testing"

	"go.thesmos.sh/core/telemetry"
	"go.thesmos.sh/testkit/bindings"
	"go.thesmos.sh/testkit/suite"
)

// AssertReporterContract runs conformance assertions against
// implementations of [telemetry.Reporter] produced by factory.
//
//	Default subtests: 4 across 4 methods
//	Shapes detected:  Pure (Counter, Gauge, Histogram, Tracer)
//	Directives:       none
//	Plug-in points:   ReporterOnCounter, ReporterOnGauge, ReporterOnHistogram, ReporterOnTracer, ReporterOnAll, ReporterCustom
func AssertReporterContract(
	t *testing.T,
	factory func() telemetry.Reporter,
	opts ...ReporterOption,
) {
	t.Helper()
	cfg := newReporterConfig(opts...)

	runReporterCounter(t, factory, &cfg)
	runReporterGauge(t, factory, &cfg)
	runReporterHistogram(t, factory, &cfg)
	runReporterTracer(t, factory, &cfg)

	for _, a := range cfg.onAll {
		cctx := suite.CrossContext[telemetry.Reporter]{
			T:             t,
			CrossBindings: bindings.CrossBindings[telemetry.Reporter]{Factory: factory},
		}
		a(cctx)
	}

	for _, custom := range cfg.custom {
		t.Run(custom.name, func(t *testing.T) {
			t.Parallel()
			custom.fn(t, factory())
		})
	}
}

func runReporterCounter(t *testing.T, factory func() telemetry.Reporter, cfg *reporterConfig) {
	t.Helper()

	t.Run("Counter/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: Counter panicked on zero-value args (%v) — supply sample values via ReporterOnCounter", r)
			}
		}()
		s := factory()
		_ = s.Counter(telemetry.InstrumentSpec{})
	})
	if len(cfg.onCounter) > 0 {
		t.Run("Counter", func(t *testing.T) {
			prePopFactory := func() telemetry.Reporter {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[telemetry.Reporter, telemetry.Counter]{
				T: t,
				PureBindings: bindings.PureBindings[telemetry.Reporter, telemetry.Counter]{
					Factory: prePopFactory,
					Call: func(impl telemetry.Reporter) telemetry.Counter {
						return impl.Counter(telemetry.InstrumentSpec{})
					},
				},
			}
			for _, a := range cfg.onCounter {
				a(pctx)
			}
		})
	}
}

func runReporterGauge(t *testing.T, factory func() telemetry.Reporter, cfg *reporterConfig) {
	t.Helper()

	t.Run("Gauge/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: Gauge panicked on zero-value args (%v) — supply sample values via ReporterOnGauge", r)
			}
		}()
		s := factory()
		_ = s.Gauge(telemetry.InstrumentSpec{})
	})
	if len(cfg.onGauge) > 0 {
		t.Run("Gauge", func(t *testing.T) {
			prePopFactory := func() telemetry.Reporter {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[telemetry.Reporter, telemetry.Gauge]{
				T: t,
				PureBindings: bindings.PureBindings[telemetry.Reporter, telemetry.Gauge]{
					Factory: prePopFactory,
					Call: func(impl telemetry.Reporter) telemetry.Gauge {
						return impl.Gauge(telemetry.InstrumentSpec{})
					},
				},
			}
			for _, a := range cfg.onGauge {
				a(pctx)
			}
		})
	}
}

func runReporterHistogram(t *testing.T, factory func() telemetry.Reporter, cfg *reporterConfig) {
	t.Helper()

	t.Run("Histogram/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: Histogram panicked on zero-value args (%v) — supply sample values via ReporterOnHistogram", r)
			}
		}()
		s := factory()
		_ = s.Histogram(telemetry.InstrumentSpec{})
	})
	if len(cfg.onHistogram) > 0 {
		t.Run("Histogram", func(t *testing.T) {
			prePopFactory := func() telemetry.Reporter {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[telemetry.Reporter, telemetry.Histogram]{
				T: t,
				PureBindings: bindings.PureBindings[telemetry.Reporter, telemetry.Histogram]{
					Factory: prePopFactory,
					Call: func(impl telemetry.Reporter) telemetry.Histogram {
						return impl.Histogram(telemetry.InstrumentSpec{})
					},
				},
			}
			for _, a := range cfg.onHistogram {
				a(pctx)
			}
		})
	}
}

func runReporterTracer(t *testing.T, factory func() telemetry.Reporter, cfg *reporterConfig) {
	t.Helper()

	t.Run("Tracer/smoke", func(t *testing.T) {
		t.Parallel()
		defer func() {
			if r := recover(); r != nil {
				t.Skipf("smoke: Tracer panicked on zero-value args (%v) — supply sample values via ReporterOnTracer", r)
			}
		}()
		s := factory()
		_ = s.Tracer("")
	})
	if len(cfg.onTracer) > 0 {
		t.Run("Tracer", func(t *testing.T) {
			prePopFactory := func() telemetry.Reporter {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[telemetry.Reporter, telemetry.Tracer]{
				T: t,
				PureBindings: bindings.PureBindings[telemetry.Reporter, telemetry.Tracer]{
					Factory: prePopFactory,
					Call: func(impl telemetry.Reporter) telemetry.Tracer {
						return impl.Tracer("")
					},
				},
			}
			for _, a := range cfg.onTracer {
				a(pctx)
			}
		})
	}
}

// ReporterOption configures [AssertReporterContract].
type ReporterOption func(*reporterConfig)

// ReporterPrePopulate runs before subtests that need pre-populated state.
// Called once per subtest against a fresh impl from the factory.
func ReporterPrePopulate(fn func(ctx context.Context, s telemetry.Reporter)) ReporterOption {
	return func(c *reporterConfig) { c.prePopulate = fn }
}

// ReporterCustom adds a free-form subtest to the contract. Use this for
// contracts not expressible via shape-specific primitives.
func ReporterCustom(name string, fn func(*testing.T, telemetry.Reporter)) ReporterOption {
	return func(c *reporterConfig) {
		c.custom = append(c.custom, reporterCustomSubtest{name: name, fn: fn})
	}
}

// ReporterOnCounter adds plug-in assertions for the Counter method.
func ReporterOnCounter(assertions ...suite.PureAssertion[telemetry.Reporter, telemetry.Counter]) ReporterOption {
	return func(c *reporterConfig) {
		c.onCounter = append(c.onCounter, assertions...)
	}
}

// ReporterOnGauge adds plug-in assertions for the Gauge method.
func ReporterOnGauge(assertions ...suite.PureAssertion[telemetry.Reporter, telemetry.Gauge]) ReporterOption {
	return func(c *reporterConfig) {
		c.onGauge = append(c.onGauge, assertions...)
	}
}

// ReporterOnHistogram adds plug-in assertions for the Histogram method.
func ReporterOnHistogram(assertions ...suite.PureAssertion[telemetry.Reporter, telemetry.Histogram]) ReporterOption {
	return func(c *reporterConfig) {
		c.onHistogram = append(c.onHistogram, assertions...)
	}
}

// ReporterOnTracer adds plug-in assertions for the Tracer method.
func ReporterOnTracer(assertions ...suite.PureAssertion[telemetry.Reporter, telemetry.Tracer]) ReporterOption {
	return func(c *reporterConfig) {
		c.onTracer = append(c.onTracer, assertions...)
	}
}

// ReporterOnAll adds cross-method assertions that span multiple methods.
func ReporterOnAll(assertions ...suite.CrossMethodAssertion[telemetry.Reporter]) ReporterOption {
	return func(c *reporterConfig) {
		c.onAll = append(c.onAll, assertions...)
	}
}

type reporterCustomSubtest struct {
	name string
	fn   func(*testing.T, telemetry.Reporter)
}

type reporterConfig struct {
	prePopulate func(context.Context, telemetry.Reporter)
	custom      []reporterCustomSubtest
	onAll       []suite.CrossMethodAssertion[telemetry.Reporter]
	onCounter   []suite.PureAssertion[telemetry.Reporter, telemetry.Counter]
	onGauge     []suite.PureAssertion[telemetry.Reporter, telemetry.Gauge]
	onHistogram []suite.PureAssertion[telemetry.Reporter, telemetry.Histogram]
	onTracer    []suite.PureAssertion[telemetry.Reporter, telemetry.Tracer]
}

func newReporterConfig(opts ...ReporterOption) reporterConfig {
	var cfg reporterConfig
	for _, opt := range opts {
		opt(&cfg)
	}
	return cfg
}
