// Code generated by testkit stub. DO NOT EDIT.
// Source: tracer.go:37-37 (testkit stub Tracer)

package telemetrytest

import (
	"context"
	"testing"

	"go.thesmos.sh/core/telemetry"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// Compile-time interface check.
var _ telemetry.Tracer = (*TracerStub)(nil)

// --- Call types ---

// TracerStartCall records one invocation of telemetry.Tracer.Start.
// Result fields use the return-value names from the source interface when
// available; unnamed returns produce positional names (Result0, Result1, ...).
type TracerStartCall struct {
	Ctx     context.Context
	Name    telemetry.SpanName
	Opts    []telemetry.SpanOption
	Result0 context.Context
	Result1 telemetry.Span
}

// --- Per-method stubs ---

// TracerStartStub controls behavior and records calls for Start.
//
// Concurrent-test primitives inherited from [stub.MethodStub]:
//   - [stub.Recorder.WaitForN] / [stub.Recorder.WaitFor] — block until calls arrive (cond-variable, no polling)
//   - [stub.Recorder.NewGate] / [stub.Gate.Release] / [stub.Gate.ReleaseOne] — deterministic race control
//   - [stub.Recorder.OnRecord] — streaming hook fired synchronously on every call
type TracerStartStub struct {
	*stub.MethodStub[TracerStartCall]
	fn       func(context.Context, telemetry.SpanName, ...telemetry.SpanOption) (context.Context, telemetry.Span)
	fallback *tracerStartReturn
}

type tracerStartReturn struct {
	Result0 context.Context
	Result1 telemetry.Span
}

// Returns sets a fixed return value for Start.
func (s *TracerStartStub) Returns(result0 context.Context, result1 telemetry.Span) *TracerStartStub {
	s.fallback = &tracerStartReturn{Result0: result0, Result1: result1}
	return s
}

// Func sets a function override for Start.
func (s *TracerStartStub) Func(fn func(context.Context, telemetry.SpanName, ...telemetry.SpanOption) (context.Context, telemetry.Span)) *TracerStartStub {
	s.fn = fn
	return s
}

// --- TracerStub ---

// TracerStubOption configures a [TracerStub] at construction time.
type TracerStubOption func(*TracerStub)

// TracerStubStrict enables strict mode — unconfigured methods fail the test.
func TracerStubStrict() TracerStubOption {
	return func(s *TracerStub) { s.strict = true }
}

// TracerStubDelegateTo wraps a real implementation — all calls delegate to
// impl and are recorded on the outer stub.
func TracerStubDelegateTo(impl telemetry.Tracer) TracerStubOption {
	return func(s *TracerStub) {
		s.OnStart.Func(impl.Start)
	}
}

// TracerStubWithClock injects a [clock.Clock] into all per-method stubs.
// Use [clock.NewTestClock] for deterministic virtual time or a consumer's
// own clock implementation. Default is real wall-clock time.
func TracerStubWithClock(clk clock.Clock) TracerStubOption {
	return func(s *TracerStub) {
		s.OnStart.WithClock(clk)
	}
}

// TracerStubWithRandSource injects a [rand.Source] into all per-method
// stubs. Use [rand.FixedRandSource] for deterministic testing or a consumer's
// own seeded RNG. Default is [rand.DefaultRandSource] (math/rand/v2).
func TracerStubWithRandSource(src rand.Source) TracerStubOption {
	return func(s *TracerStub) {
		s.OnStart.WithRandSource(src)
	}
}

// TracerStubBenchMode disables call recording on all per-method stubs. Use
// this in benchmarks to avoid allocation overhead from accumulated call logs.
// Dispatch (Func, Returns, Faults) still works normally.
func TracerStubBenchMode() TracerStubOption {
	return func(s *TracerStub) {
		s.OnStart.BenchMode()
	}
}

// WithTracerStart sets a function override at construction time.
func WithTracerStart(fn func(context.Context, telemetry.SpanName, ...telemetry.SpanOption) (context.Context, telemetry.Span)) TracerStubOption {
	return func(s *TracerStub) { s.OnStart.Func(fn) }
}

// TracerStub is a configurable test double for [telemetry.Tracer].
type TracerStub struct {
	OnStart *TracerStartStub
	strict  bool
}

// NewTracerStub creates a stub. Pass tb for auto-verification of
// expectations at test cleanup. Pass nil for a pure stub.
func NewTracerStub(tb testing.TB, opts ...TracerStubOption) *TracerStub {
	s := &TracerStub{
		OnStart: &TracerStartStub{MethodStub: stub.NewMethodStub[TracerStartCall](tb, "Tracer.Start")},
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.OnStart.Strict()
	}
	if tb != nil {
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				s.OnStart.Verify()
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count expectations.
// Func, Returns, and Faults configuration is preserved.
func (s *TracerStub) ResetCalls() {
	s.OnStart.Reset()
}

// --- Interface method implementations ---

func (s *TracerStub) Start(ctx context.Context, name telemetry.SpanName, opts ...telemetry.SpanOption) (context.Context, telemetry.Span) {
	s.OnStart.SleepLatency()
	call := TracerStartCall{Ctx: ctx, Name: name, Opts: opts}
	if s.OnStart.fn != nil {
		r0, r1 := s.OnStart.fn(ctx, name, opts...)
		call.Result0 = r0
		call.Result1 = r1
		s.OnStart.Record(call)
		return r0, r1
	}
	if s.OnStart.fallback != nil {
		f := s.OnStart.fallback
		call.Result0 = f.Result0
		call.Result1 = f.Result1
		s.OnStart.Record(call)
		return f.Result0, f.Result1
	}
	s.OnStart.FailUnexpectedCall(call)
	s.OnStart.Record(call)
	return nil, nil
}
